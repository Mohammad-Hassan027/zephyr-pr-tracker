# ==============================================================================
# COMPATIBILITY & LOCAL DEVELOPMENT ONLY
# ==============================================================================
# CANONICAL DEPLOYMENT BLUEPRINT: render.yaml
#
# This Procfile is provided strictly for backwards compatibility with legacy
# PaaS platforms (e.g., Heroku, Dokku) and local multi-process runners (e.g., Honcho, Foreman).
# The canonical infrastructure-as-code deployment configuration is defined in `render.yaml`.
# Do not add new deployment configurations or environment settings here.
# ==============================================================================
web: cd backend && npm start

