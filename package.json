{
  "name": "zephyr-pr-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Multi-tenant event participation, referral attribution, and payment verification system",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "check:deployment": "node scripts/validate-deployment-config.js",
    "check:backend": "npm --prefix backend test",
    "check:frontend": "npm --prefix frontend run type-check && npm --prefix frontend run build",
    "check:frontend:types": "npm --prefix frontend run type-check",
    "check:frontend:build": "npm --prefix frontend run build",
    "check:all": "npm run check:deployment && npm run check:backend && npm run check:frontend",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev"
  }
}
